---
title: Overview
---

The Saga Word add-in brings Saga's AI into Microsoft Word. Instead of switching between Word and the browser, you can chat with the AI about the document you're working on, run tasks like proofreading and summarization, and apply suggestions directly — all from a sidebar inside Word.

## What you can do

The add-in opens as a task pane on the right side of Word. From there you have access to:

**Chat** — Have a conversation with the AI about the document you're editing. The AI automatically reads the current document as context, so you can ask questions about its content, request analysis, or draft new sections — without pasting anything manually.

**Tasks** — Run built-in tasks on the document or selected text:

- `/proofread` — check grammar, spelling, and style
- `/anonymize` — redact personal identifiers and sensitive information
- `/summarize` — generate a concise summary
- `/compare` — compare sections or versions
- `/auto-fill` — fill in Word templates from source documents (add-in only)

**Answering modes** — Control how the AI responds:

- **Ask** — the AI analyzes and answers without suggesting edits
- **Suggest** — the AI returns structured suggestions you can apply directly to the document
- **Auto** — the AI decides based on your request whether to analyze or suggest edits

When the AI returns suggestions in **Suggest** mode, you can review each one and apply it to the document with a click — no copy-pasting between tools.

**Apply options** — When applying a suggestion, you choose how it lands in the document:

| Option | What it does |
|--------|-------------|
| **Direct** | Replaces the original text immediately |
| **Comment** | Adds the suggestion as a Word comment on the relevant passage |
| **Tracked change** | Inserts the edit as a tracked change so you can accept or reject it later |
| **Tracked change with comment** | Inserts a tracked change and attaches the AI's reasoning as a comment |

You can set a default apply option in the add-in settings so you don't have to pick one every time. Open the add-in sidebar, go to **Settings**, and choose your preferred option under **Default apply option**. If no default is set, the add-in asks you each time.

## Supported versions

| Platform | Supported versions |
|----------|-------------------|
| **Microsoft 365 (Desktop)** | Word for Microsoft 365 on Windows and Mac |
| **Perpetual licenses** | Word 2024, Word 2021, Word 2019, Word 2016 |
| **Word for the Web** | Supported via browser |

To get started, see [User setup](/guides/integrations/word-add-in/user-setup) for installation and sign-in, or [Admin setup](/guides/integrations/word-add-in/admin-setup) for enterprise deployment.

<Note>
Some Volume License (LTSC) editions may have limited access to the Office Add-in Store. If the add-in doesn't appear, see [Admin setup](/guides/integrations/word-add-in/admin-setup) for enterprise deployment options.
</Note>
